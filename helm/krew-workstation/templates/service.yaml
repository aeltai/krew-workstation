apiVersion: v1
kind: Service
metadata:
  name: {{ include "krew-workstation.fullname" . }}
  labels:
    {{- include "krew-workstation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krew-workstation.selectorLabels" . | nindent 4 }}
